# C3: CUDA Graphs OFF (Enforce Eager)
# Comparison: Measures CUDA graph compilation overhead vs baseline

name: C3
description: "vLLM with CUDA graphs disabled (--enforce-eager)"

model_id: "mistralai/Mistral-7B-Instruct-v0.3"
served_model_name: "mistral7b"
max_model_len: 8192
gpu_memory_utilization: 0.90

extra_args:
  - "--enforce-eager"

env:
  HUGGING_FACE_HUB_DISABLE_TELEMETRY: "1"

